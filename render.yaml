services:
  - name: backend
    type: web
    buildCommand: npm run tsc
    startCommand: npm run start
    env:
      - key: PORT
        value: "7777"

  - name: frontend
    type: static
    buildCommand: npm run build && cp -r build ../server/
    startCommand: npm run start
    env:
      - key: PORT
        value: "3000"